const { contextBridge, ipcRenderer } = require('electron');
contextBridge.exposeInMainWorld('zelux', { version: '1.6.0', platform: process.platform, window: { minimize: () => ipcRenderer.send('window-minimize'), maximize: () => ipcRenderer.send('window-maximize'), close: () => ipcRenderer.send('window-close') }, systemStats: () => ipcRenderer.invoke('system-stats'), startDownload: urls => ipcRenderer.invoke('download-start', urls), onDownloadOutput: callback => ipcRenderer.on('download-output', (_, value) => callback(value)), onDownloadFinished: callback => ipcRenderer.on('download-finished', (_, code) => callback(code)) });
